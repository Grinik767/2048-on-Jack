class Main {
     function void main () {
         var Board gameBoard;
         var Array a, b;
         let gameBoard = Board.new();
         let a = Array.new(4);
         let a[0] = Tile.new(2);
         let a[1] = Tile.new(2);
         let a[2] = Tile.new(4);
         let a[3] = Tile.new(0);
         let b = gameBoard.CalculateLine(a);
         do Display.printArray(b);
         return;
     }
}